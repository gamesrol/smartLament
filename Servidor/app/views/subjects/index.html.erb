<h1>Listing subjects</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subjects.each do |subject| %>
      <tr>
        <td><%= subject.name %></td>
        <td><%= link_to 'Mostrar', subject %></td>
        <td><%= link_to 'Editar', edit_subject_path(subject) %></td>
        <td><%= link_to 'Eliminar', subject, method: :delete, data: { confirm: '¿Est&aacute;s seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subject', new_subject_path %>
