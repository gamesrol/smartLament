<h1>Lista de los &Aacute;mbitos</h1>
<div class="col-lg-12">
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scopes.each do |scope| %>
      <tr>
        <td><%= scope.name %></td>
        <td><%= link_to 'Mostrar', scope %></td>
        <td><%= link_to 'Editar', edit_scope_path(scope) %></td>
        <td><%= link_to 'Eliminar', scope, method: :delete, data: { confirm: '¿Est&aacute;s seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>

<%= link_to 'New Scope', new_scope_path %>
